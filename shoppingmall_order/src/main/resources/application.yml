server:
  port: 9000

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/billmall_order
    driver-class-name: com.mysql.jdbc.Driver
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto